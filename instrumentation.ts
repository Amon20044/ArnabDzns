export function register() {
  // Keep runtime-specific setup out of this shared instrumentation entrypoint.
  // Next evaluates this file for Edge too, so Node-only imports belong in the
  // modules that actually need them.
}
